# Sprint S04 — HomeMap Component

**Status**: ⬜ todo
**Workplan tasks**: P1-T01, P1-T03, P1-T04, P1-T05
**Estimate**: 20h
**Depends on**: S01, S02 done

## Scope
- `Nav.tsx`, `Footer.tsx`, `Layout.tsx`, theme tokens (CSS vars brand colors)
- `HomeMap.tsx` — Google Maps JS API (lazy load), pin clusters, geolocation
- Mode toggle: 🏠 Mua/Bán | 🔑 Cho thuê — persist `?mode=` trong URL
- `/api/projects/by-bounds` — query projects trong viewport bounds theo mode
- `PinMarker.tsx` — custom SVG pin, màu theo tier (sale) hoặc demand_score (rent), label giá
- `ProjectBottomSheet.tsx` — mobile snap sheet, content switch theo mode
- `SearchBox.tsx` — autocomplete với tsvector/trigram

## Out of scope
- Không làm filter sidebar (Sprint S06)
- Không làm project hub page (Sprint S05)

## Context tao cần đọc đầu session
- `sprints/S04.md`
- `CLAUDE.md` (phần Transaction Modes + Maps)
- `UI_map.png` — xem lại UI mockup
- `schema.sql` chỉ phần projects core fields (lines ~125-170)

## Done when
- Map load < 3s mobile
- Pin hiện theo viewport bounds
- Toggle mode → pin label đổi (tr/m² vs tr/tháng)
- Click pin → bottom sheet hiện đúng data theo mode
- Search "vinhom" → autocomplete ra kết quả
